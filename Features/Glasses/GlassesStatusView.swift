import SwiftUI

struct GlassesStatusView: View {
    var body: some View {
        NavigationStack {
            Text("Glasses status placeholder")
                .navigationTitle("Glasses")
        }
    }
}
